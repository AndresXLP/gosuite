package config_test

const (
	hostDirShouldMsg            = "HOST_DIR should be '%v' and is %v"
	appPortShouldMsg            = "APP_PORT should be '%v' and is %v"
	appServiceNameShouldMsg     = "APP_SERVICE_NAME should be '%v' and is %v"
	appIntervalTimeOutShouldMsg = "APP_INTERVAL_TIME_OUT should be '%v' and is %v"
	appRequiredShouldMsg        = "APP_REQUIRED should be '%v' and is %v"
	shouldNotBeError            = "It shouldn't be a error and it is %v"
	shouldBeError               = "It should be a error and it is %v"
)
